{% capture content %}
<div class="container">
  <div class="row row--mobile-collapse {% if image_position == "right" %}row--reverse{% endif %}">

    <div class="eight columns medium-down--one-whole">
      <div class="content-section--image-text-columns__image-wrapper">
        {{ image | img_url: 'x500' | img_tag }}
      </div>
    </div>

    <div class="eight columns medium-down--one-whole">
      <div class="content-section--image-text-columns__text-wrapper">
        <div class="content-section--image-text-columns__text">
          {% if title != blank %}
            {% include "content-section.title" with {
              title: title | escape,
              id: id,
              title_tag: title_tag
            } %}
          {% endif %}

          {{ text_content }}
        </div>
      </div>
    </div>

  </div>
</div>
{% endcapture %}

{% include 'content-section' with {
  id: id,
  id_classes: "image-with-text"
  modifiers: modifiers,
  content: content
} %}
